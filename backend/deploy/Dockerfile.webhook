FROM golang:1.25-alpine AS builder

WORKDIR /app
COPY webhook.go .
RUN go mod init webhook && CGO_ENABLED=0 GOOS=linux go build -ldflags="-s -w" -o /webhook .

FROM alpine:3.21

RUN apk --no-cache add git docker-cli
COPY --from=builder /webhook /webhook

EXPOSE 9000

ENTRYPOINT ["/webhook"]
